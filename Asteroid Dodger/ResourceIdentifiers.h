#ifndef ResourceIdentifiers_H_
#define ResourceIdentifiers_H_

namespace Texture {
	enum ID { Player, AsteroidMap };
}

namespace Font {
	enum ID { GameFont };
}

namespace Sound {
	enum ID { SpaceshipThruster };
}

namespace Music {
	enum ID { GameTheme };
}
#endif